<!DOCTYPE FilterScript>
<FilterScript>
 <filter name="Remove Isolated pieces (wrt Face Num.)">
  <Param tooltip="Delete all the connected components (floating pieces) composed by a number of triangles smaller than the specified one" description="Enter minimum conn. comp size:" type="RichInt" value="25" name="MinComponentSize"/>
 </filter>
 <filter name="Remove Unreferenced Vertex"/>
</FilterScript>
